import {
  StyleSheet,
} from 'react-native';

export const styles = StyleSheet.create({
  
  cellContainer: {
    flexDirection: 'row',
    alignContent: 'flex-start',
    alignItems: 'center',
  },

  avatar:{
    width: 58,
    height: 58,
    marginVertical: 18,
    marginHorizontal: 26,
    borderWidth: 0.2,
    borderRadius: 28,
  },
  
});